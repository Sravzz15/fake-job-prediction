#Task 1 – Personalize the Web App

#You’ll make the UI more appealing and add new sections.

#🔹 Step 1: Modify the UI (colors, fonts, button style)

#Edit your templates/index.html (or your main form page):

<!DOCTYPE html>
<html>
<head>
    <title>Fake Job Detection</title>
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background: #f4f8fb;
            color: #333;
            text-align: center;
            margin: 0;
            padding: 0;
        }

        h1 {
            background-color: #0078d7;
            color: white;
            padding: 20px;
            margin-bottom: 30px;
            border-radius: 0 0 20px 20px;
        }

        form {
            background: white;
            display: inline-block;
            padding: 30px;
            border-radius: 15px;
            box-shadow: 0 0 10px rgba(0,0,0,0.2);
            width: 60%;
        }

        textarea {
            width: 90%;
            height: 150px;
            border-radius: 8px;
            border: 1px solid #ccc;
            padding: 10px;
            font-size: 16px;
        }

        button {
            background-color: #0078d7;
            color: white;
            border: none;
            padding: 12px 30px;
            font-size: 16px;
            border-radius: 8px;
            cursor: pointer;
        }

        button:hover {
            background-color: #005fa3;
        }

        /* New section: Tips box */
        .tips {
            background-color: #e9f6ff;
            border-left: 6px solid #0078d7;
            margin: 30px auto;
            width: 60%;
            padding: 15px;
            border-radius: 10px;
            text-align: left;
        }

        footer {
            background-color: #0078d7;
            color: white;
            padding: 10px;
            margin-top: 30px;
            font-size: 14px;
        }
    </style>
</head>
<body>
    <h1>Fake Job Detection System</h1>

    <form action="/predict" method="post">
        <textarea name="job_description" placeholder="Paste job description here..."></textarea><br><br>
        <button type="submit">Check Job</button>
    </form>

    <!-- ✅ New Tips Section -->
    <div class="tips">
        <h3>💡 Tips to Avoid Fake Jobs:</h3>
        <ul>
            <li>Beware of jobs asking for money upfront.</li>
            <li>Check company domain emails (avoid Gmail/Yahoo for corporate jobs).</li>
            <li>Verify company details on official websites or LinkedIn.</li>
            <li>Avoid offers that promise huge salaries with minimal work.</li>
        </ul>
    </div>

    <!-- Footer -->
    <footer>
        Developed by <b>Your Name / Team Name</b> | Project: <b>Fake Job Detection</b>
    </footer>
</body>
</html>


#This achieves:

#New colors and modern font

#A “Tips” box below form

#Footer with your name/team name and project title

# Task 2 – Add Confidence Display

#On your result page (e.g., templates/result.html), display the confidence level.

#Assume your Flask backend sends two variables:

#prediction → “Real” or “Fake”

#confidence → e.g., 0.87 (87%)

#result.html
<!DOCTYPE html>
<html>
<head>
    <title>Job Prediction Result</title>
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f4f8fb;
            text-align: center;
            padding: 40px;
        }
        h2 {
            color: #0078d7;
        }
        progress {
            width: 60%;
            height: 25px;
        }
        footer {
            background-color: #0078d7;
            color: white;
            padding: 10px;
            margin-top: 30px;
            font-size: 14px;
        }
    </style>
</head>
<body>
    <h2>Prediction Result</h2>

    <p><strong>Job Type:</strong> {{ prediction }}</p>

    <p><strong>Confidence Level:</strong> {{ (confidence * 100) | round(2) }}%</p>
    <progress value="{{ confidence * 100 }}" max="100"></progress>

    <br><br>
    <a href="/">← Back to Home</a>

    <footer>
        Developed by <b>Your Name / Team Name</b> | Project: <b>Fake Job Detection</b>
    </footer>
</body>
</html>
#Flask Backend (update for confidence)

#In your app.py (Flask file), ensure you send confidence to the template:

from flask import Flask, render_template, request
import joblib

app = Flask(__name__)

model = joblib.load('model.pkl')
vectorizer = joblib.load('vectorizer.pkl')

@app.route('/')
def home():
    return render_template('index.html')

@app.route('/predict', methods=['POST'])
def predict():
    job_desc = request.form['job_description']
    input_features = vectorizer.transform([job_desc])
    prediction = model.predict(input_features)[0]
    confidence = model.predict_proba(input_features)[0].max()  # highest class probability

    return render_template('result.html', prediction=prediction, confidence=confidence)

if __name__ == '__main__':
    app.run(debug=True)
#Task 1 – UX Improvement
# Goal:

#Add a colored badge or progress bar for prediction confidence

#Show “Last Prediction Result” on the index page

#Add Tips to Identify Fake Jobs

# Step 1: Update app.py

#We’ll store the last prediction and send it to the index page.

from flask import Flask, render_template, request
import joblib

app = Flask(__name__)

model = joblib.load('model.pkl')
vectorizer = joblib.load('vectorizer.pkl')

last_prediction = None  # store last result globally

@app.route('/')
def home():
    return render_template('index.html', last_prediction=last_prediction)

@app.route('/predict', methods=['POST'])
def predict():
    global last_prediction
    job_desc = request.form.get('job_description', '').strip()

    # Validation moved to Task 2 below (we’ll add it soon)
    if not job_desc:
        return render_template('index.html', error="Please enter a job description!")

    input_features = vectorizer.transform([job_desc])
    prediction = model.predict(input_features)[0]
    confidence = model.predict_proba(input_features)[0].max()

    last_prediction = {
        'prediction': prediction,
        'confidence': round(confidence * 100, 2)
    }

    return render_template('result.html', prediction=prediction, confidence=confidence)
    

if __name__ == '__main__':
    app.run(debug=True)

#Step 2: Modify templates/index.html

#Add:

#“Last Prediction Result” box

#“Tips to Identify Fake Jobs” section

#Styled alerts for errors

<!DOCTYPE html>
<html>
<head>
    <title>Fake Job Detection</title>
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f4f8fb;
            text-align: center;
            padding: 20px;
        }
        h1 {
            color: #0078d7;
        }
        form {
            background: white;
            padding: 30px;
            border-radius: 15px;
            width: 60%;
            margin: auto;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }
        textarea {
            width: 90%;
            height: 150px;
            padding: 10px;
            border-radius: 8px;
            border: 1px solid #ccc;
            font-size: 16px;
        }
        button {
            background-color: #0078d7;
            color: white;
            border: none;
            padding: 12px 30px;
            border-radius: 8px;
            font-size: 16px;
            cursor: pointer;
        }
        button:hover {
            background-color: #005fa3;
        }
        .tips {
            background-color: #e9f6ff;
            border-left: 5px solid #0078d7;
            width: 60%;
            margin: 30px auto;
            padding: 15px;
            border-radius: 10px;
            text-align: left;
        }
        .last-result {
            width: 60%;
            margin: 20px auto;
            background: #fff;
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
        }
        .badge {
            padding: 6px 12px;
            border-radius: 6px;
            color: white;
            font-weight: bold;
        }
        .fake { background-color: #e74c3c; } /* red */
        .real { background-color: #27ae60; } /* green */
        .alert {
            color: white;
            background-color: #e74c3c;
            padding: 10px;
            border-radius: 6px;
            width: 60%;
            margin: 10px auto;
        }
        footer {
            margin-top: 40px;
            background-color: #0078d7;
            color: white;
            padding: 10px;
        }
    </style>
    <script>
        function validateForm() {
            const text = document.forms["jobForm"]["job_description"].value.trim();
            if (text === "") {
                alert("⚠️ Please enter a job description before submitting!");
                return false;
            }
            if (/^[^a-zA-Z]+$/.test(text)) {
                alert("⚠️ Please enter valid text (not just numbers or symbols)!");
                return false;
            }
            return true;
        }
    </script>
</head>
<body>
    <h1>Fake Job Detection System</h1>

    {% if error %}
        <div class="alert">{{ error }}</div>
    {% endif %}

    <form name="jobForm" action="/predict" method="post" onsubmit="return validateForm()">
        <textarea name="job_description" placeholder="Paste job description here..."></textarea><br><br>
        <button type="submit">Check Job</button>
    </form>

    <!-- ✅ Last Prediction Result -->
    {% if last_prediction %}
    <div class="last-result">
        <h3>🕒 Last Prediction Result</h3>
        <p>
            <strong>Job Type:</strong>
            <span class="badge {{ 'fake' if last_prediction.prediction == 'Fake' else 'real' }}">
                {{ last_prediction.prediction }}
            </span>
        </p>
        <p><strong>Confidence:</strong> {{ last_prediction.confidence }}%</p>
        <progress value="{{ last_prediction.confidence }}" max="100" style="width:80%; height:20px;"></progress>
    </div>
    {% endif %}

    <!-- ✅ Tips Section -->
    <div class="tips">
        <h3>💡 Tips to Identify Fake Jobs:</h3>
        <ul>
            <li>Beware of jobs asking for money upfront.</li>
            <li>Check the company website and LinkedIn profile.</li>
            <li>Watch for poor grammar or suspicious emails.</li>
            <li>Too-good-to-be-true offers are usually fake.</li>
        </ul>
    </div>

    <footer>
        Developed by <b>Your Name / Team Name</b> | Project: <b>Fake Job Detection</b>
    </footer>
</body>
</html>

#Step 3: Result Page (Optional Enhancement)

#You can reuse the progress bar + color badges on result.html:

<h2>Prediction Result</h2>
<p><strong>Job Type:</strong>
    <span class="badge {{ 'fake' if prediction == 'Fake' else 'real' }}">{{ prediction }}</span>
</p>
<p><strong>Confidence:</strong> {{ (confidence * 100) | round(2) }}%</p>
<progress value="{{ confidence * 100 }}" max="100" style="width:80%; height:20px;"></progress>

#Task 2 – Error Handling & Edge Cases
# Goal:

#Handle empty input

#Handle invalid (non-text) input

#Display user-friendly alerts

#Already added JavaScript validation (validateForm()):

#Shows alert if the text is empty or only symbols/numbers.
# Also handled backend-side empty input in Flask (using if not job_desc condition).

